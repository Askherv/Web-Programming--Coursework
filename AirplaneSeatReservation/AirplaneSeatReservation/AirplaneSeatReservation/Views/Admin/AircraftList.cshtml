@model List<AirplaneSeatReservation.Models.Aircraft>
@{
    ViewData["Title"] = "AircraftList";
    Layout = "~/Views/Shared/_AdminPanelLayout.cshtml";
}

<div id="page-wrapper">
	<div class="main-page">
		<h3 class="title1">Tüm Uçaklar</h3>
		<div class="blank-page widget-shadow scroll" id="style-2 div1">
			<section class="section__container booking__container">
				<div class="booking__nav">
					<span>Uçak Bilgileri</span>
				</div>
				<style>
					table {
						border-collapse: collapse;
						width: 100%;
						margin-top: 20px;
					}

					th, td {
						border: 1px solid #dddddd;
						text-align: left;
						padding: 8px;
					}

					th {
						background-color: #f2f2f2;
					}

					tr:nth-child(even) {
						background-color: #f9f9f9;
					}

					tr:hover {
						background-color: #e0e0e0;
					}
				</style>
				<table border="1">
					<thead>
						<tr>
							<td>Uçak No</td>
							<td>Uçak Modeli</td>
							<td>Koltuk Sayısı</td>
						</tr>
					</thead>
					<tbody>
						@if (Model != null)
						{
							@foreach (var aircrafts in Model)
							{
								<tr>
									<td>@aircrafts.AircraftID</td>
									<td>@aircrafts.AircraftModel</td>
									<td>@aircrafts.AircraftSeats</td>
								</tr>
							}
						}
					</tbody>
				</table>
			</section>
		</div>
	</div>
</div>

